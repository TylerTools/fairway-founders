# Agent log — 2026-07

_Rotated from AGENTS.md Recent log. Newest first._

- **2026-07-09 — @claude-design (per Tyler):** Orphan-request rescue. Users who signed up outside `/join/[slug]` never got a `league_memberships` row and were invisible in `/admin/access`. New "Waiting for a league" section surfaces them + `attachOrphanToLeague`/`denyOrphanUser` actions; `addLeagueMember` now also flips `access_status='approved'` so the GLN picker can rescue orphans too; `/gln/leagues/[id]` candidate list widened to include pending users. Cockpit KPI counts orphans.
